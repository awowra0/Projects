# Decision Tree
This is a simplified model of decision tree that tries to classify data by its attributes.  
Order of the attributes bases on entrophy and information gain.  
In the end, a text file with decisions is made.

# How to run
Move data file to program's folder.  
Write in terminal 'python -c "import DT; DT.main(your_filename)"'